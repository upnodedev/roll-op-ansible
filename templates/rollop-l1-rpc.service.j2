[Unit]
Description=Ansible controller
After=multi-user.target

[Service]
Type=simple
Restart=always
User={{ ansible_user }}
Group={{ ansible_user }}
WorkingDirectory={{user_dir}}/roll-op
ExecStart=./rollop l1

[Install]
WantedBy=multi-user.target