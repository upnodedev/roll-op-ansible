[Unit]
Description=Rollop l2
After=multi-user.target

[Service]
Type=simple
Restart=always
User={{ ansible_user }}
Group={{ ansible_user }}
WorkingDirectory={{ rollop_user_dir }}
ExecStart=bash {{ rollop_user_dir }}/start-rollop.sh

[Install]
WantedBy=multi-user.target